[package]
name = "phylaxis-graph"
description = "Call graph, data-flow graph and source-to-sink reachability."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
phylaxis-core.workspace = true
petgraph.workspace = true
thiserror.workspace = true

[dev-dependencies]
# Tests build package graphs from inline Python; the parser is the only way to get an Ast.
# A dev-dependency does not invert the runtime boundary (ARCHITECTURE.md).
phylaxis-parse.workspace = true
